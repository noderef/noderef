# trashcan_list

- Lists deleted nodes (`GET /deleted-nodes`) with paging.
- Optional `include` follows Alfresco list parameters (e.g. extra metadata).
- Use returned node ids with `trashcan_restore` / `trashcan_purge`.
