# tag_list

- With `nodeId`: lists tags on that node (`GET /nodes/{nodeId}/tags`).
- Without `nodeId`: lists global tags (`GET /tags`); optional `tag` + `matching` use Alfresco tag filter helpers in the client.
