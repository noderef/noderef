# association_list

- `parents`: parent folders (`GET .../parents`).
- `secondary_children`: secondary children (`GET .../secondary-children`).
- `targets`: peer target associations (`GET .../targets`).
- `sources`: incoming associations (`GET .../sources`). Paging may be ignored where the API has no skip/max.
