# version_revert

- Reverts content to a prior version (`POST .../versions/{versionId}/revert`). Requires confirmation.
- Use `node_versions` first to obtain `versionId`. Optional `majorVersion` and `comment`.
