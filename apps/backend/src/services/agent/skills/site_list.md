# site_list

- Lists Alfresco sites (`GET /sites`) with `skipCount` / `maxItems`.
- Optional `where` uses Alfresco predicate syntax (see Core API “List sites”).
- Read `pagination.totalCount` for how many sites match (not just the page length).
