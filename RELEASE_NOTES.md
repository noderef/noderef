This release introduces **node permissions management** and **users/groups management**, including group member handling. Bug fixes include improved **desktop clipboard handling** in Node Browser and Search Query Builder, a combobox portal positioning fix, and release automation updates for more reliable changelog issue detection across merge strategies. It also includes dependency updates and an RPC import refactor to static imports.
