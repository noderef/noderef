This release introduces **Docker deployment support** with a new server automatically configured during bootstrap. The **Node Browser** now features **desktop clipboard support**. The File/Folder Browser gets a **resizable name column** with persistence, improving usability for long file names. We've also added  **backend integration tests** for improved reliability. Bug fixes include removal of deprecated `isProbablyOnline` usage.
