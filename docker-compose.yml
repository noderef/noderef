services:
  noderef:
    build:
      context: .
    image: noderef:latest
    ports:
      - "5001:5001"
    environment:
      PORT: 5001
      DEBUG: "0"
      DATA_DIR: /data
    volumes:
      - noderef_data:/data

volumes:
  noderef_data:
